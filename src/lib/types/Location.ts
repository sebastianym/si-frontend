export interface Location {
    id: number;
    faculty: string;
    location_name: string;
    code: string;
    address: string;
    createdAt: string;
    updatedAt: string;
    is_active: boolean;
}